import { PagePlaceholder } from "../components/Placeholder";

export default function ComplaintMigrationPage() {
  return (
    <PagePlaceholder
      title="Complaint Migration"
      description="Review open complaints affected by a proposed jurisdiction change, compare current vs proposed responsibility and apply the migration with a full audit trail."
      note="P2"
    />
  );
}