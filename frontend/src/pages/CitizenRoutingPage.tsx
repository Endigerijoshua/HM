import { PagePlaceholder } from "../components/Placeholder";

export default function CitizenRoutingPage() {
  return (
    <PagePlaceholder
      title="Citizen Routing"
      description="Pick a location on the map and an issue type to see the responsible authority, department and service - with a 'Why this route?' explanation."
      note="P1"
    />
  );
}